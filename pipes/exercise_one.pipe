TOKEN "exercise_one_endpoint_read_8947" READ
TOKEN "read_token" READ

NODE exercise_one_0
SQL >
    %
    SELECT count() as trips FROM yellow_tripdata where passenger_count ={{ Int16(passenger_count, 2) }}
